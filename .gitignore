# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# runtime data (durable store + generated runtime evidence)
/data/*.db
/data/*.db-*
/runtime/reports/*.json

# secrets / env (credential discovery output)
.env
.env.local
.env*.local
.dev.vars

# internal hackathon-workflow artifacts (not for public repo)
/.hunter/
/.impeccable/
/pitch/
# media build scratch (ppt-master project + venv, hyperframes workspace)
/.ppt-build/
/.video-build/
# typescript incremental build info
tsconfig.tsbuildinfo

# generated media / evidence / deck / posters / video scratch (not source code)
/docs/evidence/
/docs/deck/
/docs/posters/
/docs/screenshots/
/docs/video/
/docs/brand/

# stray credential files (defense in depth; real keys live in $HOME)
user_key.txt
use_key.txt

# os
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.idea
.vscode
